<!-- <% layout("/layouts/boilerplate") %>
<div class="row mt-3">
    <div class="col-8 offset-2">
        <h2>Booking Details</h2>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= booking.listing.title %></h5>
                <h6 class="card-subtitle mb-2 text-muted">Booked by: <%= booking.user.username %></h6>
                <p class="card-text">Start Date: <%= new Date(booking.startDate).toLocaleDateString() %></p>
                <p class="card-text">End Date: <%= new Date(booking.endDate).toLocaleDateString() %></p>
                <p class="card-text">Number of Guests: <%= booking.guests %></p>
                <p class="card-text"><small class="text-muted">Booked on: <%= new Date(booking.createdAt).toLocaleDateString() %></small></p>
                <a href="/listings/<%= booking.listing._id %>" class="btn btn-primary">Back to Listing</a>
                <% if (booking.user._id.equals(currentUser._id)) { %>
                    <form class="d-inline" action="/listings/<%= booking.listing._id %>/bookings/<%= booking._id %>?_method=DELETE" method="POST">
                        <button class="btn btn-danger">Cancel Booking</button>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</div> -->